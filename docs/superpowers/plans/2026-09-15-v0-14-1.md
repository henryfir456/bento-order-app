# v0.14.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement only the three v0.14.1 fixes: canonical non-LINE delegated-order candidates, historical order metadata in personal ledger projections, and immediate owner-scoped balance synchronization after order mutations.

**Architecture:** Keep Worker + D1 as the production source of truth. Preserve existing delegated-order authorization and ledger accounting semantics. Correct the canonical user projection so a missing LINE binding is not modeled as an `EMPLOYEE_GUEST` principal, enrich ledger reads from the order's historical snapshot tables, and consume the existing mutation `newBalance` result for the actual balance owner in React state.

**Tech Stack:** Cloudflare Worker, D1/SQLite, React, Vite, Node test runner, ESLint.

**Spec:** User-provided v0.14.1 requirements in this task; no separate spec file.

## Global Constraints

- Do not deploy, migrate remote D1, modify production data, commit, push, or perform unrelated refactors.
- Preserve existing Admin / ProxyAdmin / User authorization policy and View As identity separation.
- Never join current menu data to render historical order details; use order and order-item snapshots.
- Report baseline failures separately from regressions and distinguish automated from manual verification.

## Task 1: Canonical delegated-order candidate projection

**Files:** `worker-poc/src/db/users.js`, `src/features/balances/identityStatus.js`, `src/components/IdentityStatusBadges.jsx`, `src/App.jsx`, `worker-poc/tests/orders-delegated.test.js`, `tests/admin-identity-ui.test.cjs`.

- [x] Add regression coverage for an active profile-complete canonical member with `line_user_id = NULL`, plus inactive and historical provisional garbage rows, and assert the non-LINE member is included with a non-guest semantic badge.
- [x] Confirm frontend candidate loading and rendering do not filter `line_user_id`; add coverage for the new projection/badge mapping.
- [x] Change only the canonical public-user projection and its presentation mapping so a non-LINE canonical member is represented as an employee/canonical member, not an `EMPLOYEE_GUEST` authentication principal.
- [x] Run the delegated Worker and related frontend tests.

## Task 2: Historical order metadata in ledger projection

**Files:** `worker-poc/src/db/ledgerQueries.js`, `worker-poc/src/domain/ledger.js`, `src/App.jsx`, `worker-poc/tests/balance-history.test.js`, `tests/cancel-flow.test.cjs`.

- [x] Add failing regression coverage for Chinese `ORDER_CREATED` / `ORDER_CANCELLED` descriptions, date/vendor/items/quantities, multi-item orders, and historical snapshot names after current-menu changes.
- [x] Read the referenced order and its `order_items.item_name_snapshot` / quantity data in a bounded ledger projection query without changing ledger amounts or accounting meaning.
- [x] Render the structured order metadata in the personal transaction detail UI.
- [x] Run the relevant Worker history/ledger tests and frontend cancellation/history tests.

## Task 3: Owner-scoped authoritative balance synchronization

**Files:** `src/App.jsx`, `tests/cancel-flow.test.cjs`, `worker-poc/tests/orders-create.test.js`, `worker-poc/tests/orders-cancel.test.js`.

- [x] Add failing coverage requiring create and cancel success handlers to consume authoritative `newBalance` for self mutations and target-member state for delegated mutations.
- [x] Update the self mutation path to synchronize the canonical authenticated/header user balance from the server result; retain target-only updates for proxy mutations.
- [x] Verify Worker mutation responses already identify the owner-scoped authoritative balance and do not alter ledger ownership or amounts.
- [x] Run all related Worker mutation and frontend tests.

## Task 4: Release metadata and verification

**Files:** `package.json`, `package-lock.json`, `CHANGELOG.md`, `src/data/changelog.js`, affected release regression tests.

- [x] Set the application release to `0.14.1` and add the requested English changelog entry plus synchronized Chinese UI release text.
- [x] Run the declared Worker tests, frontend tests, lint, build, and `git diff --check`.
- [x] Review the final diff for only the three requested fixes and report commit readiness without creating a commit.
